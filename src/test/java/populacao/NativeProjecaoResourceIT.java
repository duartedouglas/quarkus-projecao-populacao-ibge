package populacao;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeProjecaoResourceIT extends ProjecaoResourceTest {

    // Execute the same tests but in native mode.
}